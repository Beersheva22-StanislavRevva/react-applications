import { Typography } from "@mui/material"
import exp from "constants"

const SalaryStatistics: React.FC = () => {
    return <Typography variant = "h4" align = "center">SalaryStatistics page</Typography>
}
export default SalaryStatistics;