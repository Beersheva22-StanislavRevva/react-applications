import { Typography } from "@mui/material"
import exp from "constants"

const AgeStatistics: React.FC = () => {
    return <Typography variant = "h4" align = "center">AgeStatistics page</Typography>
}
export default AgeStatistics;