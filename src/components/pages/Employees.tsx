import { Typography } from "@mui/material"
import exp from "constants"

const Employees: React.FC = () => {
    return <Typography variant = "h4" align = "center">Employees page</Typography>
}
export default Employees;